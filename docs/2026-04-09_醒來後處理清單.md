# 2026-04-09 醒來後處理清單

> 這份是暫存交接文件。
> 你醒來照這份做完後，可以直接刪掉。

---

## 今天已完成

1. 主手冊已修正 §61 / §63 / §64，不再把 `PartyPanel`、`DriftFragmentPanel`、`GazettePanel` 誤寫成「尚未建好」
2. 已新增 `docs/COCOS_PANELS_61_63_64_PROGRAM_HANDOFF.md`
3. 已新增下一階段手冊內容 §68～§76
4. 已補一份給 COCOS 端直接照做的 `COCOS_61_63_64_節點連接手冊.md`
5. 已補一份最短版 `COCOS_NEXT_PHASE_CHECKLIST.md`

## 目前確認狀態

1. `PartyPanel.ts`、`DriftFragmentPanel.ts`、`GazettePanel.ts` 已存在
2. `MainGameController.ts` 已有三個面板的 Inspector 欄位與 show route
3. 三個面板目前仍是 Mock data，不是真 API
4. 三個面板目前沒有獨立 backdrop close 綁定
5. `party / drift / gazette` 目前不是固定左欄入口

## 你醒來後的最短順序

1. 先照 `E:\PTD-COCOS\COCOS_61_63_64_節點連接手冊.md` 把三個面板節點拖綁完
2. 完成後再照 `E:\PTD-COCOS\COCOS_NEXT_PHASE_CHECKLIST.md` 補 NPC Shop Tab 與 Fog / Glitch 空節點
3. 如果要回頭看大手冊，再打開 `docs/COCOS_COLLAB_SETUP_MANUAL.md`

## 做的時候只要記住 5 件事

1. `memberContent`、`fragmentContent`、`gazetteContent` 一律拖 `ScrollView/view/content`
2. `bgSprite` 要拖 `PanelBG` 的 `Sprite` 元件，不是節點
3. `closeButton` 要拖 `CloseButton` 節點
4. `GazettePanel` 四顆 filter 按鈕節點本身要有 `Sprite`
5. Preview 驗收先看 X 關閉、列表生成、Filter 換色、Console log，不先驗 backdrop close

## 做完後回報格式

```text
【醒來後處理回報】

1. 已完成節點
- PartyPanelNode：
- DriftFragmentPanelNode：
- GazettePanelNode：
- NPC Shop Tab：
- Fog / Glitch：

2. Inspector 拖綁
- 已全部拖好的欄位：
- 還是 None 的欄位：

3. Preview 結果
- 可正常開關的面板：
- Console 出現的 log：
- 還沒過的點：

4. 下一步
- 我準備先補哪個系統：
```